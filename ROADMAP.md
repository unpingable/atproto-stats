# Roadmap

## Near Term

- Improve report readability (focus mode + compact mode)
- Keep sync idempotence and run-receipt quality visible
- Harden docs and packaging for easier contribution

## Next

- Add CSV export for top-by-score/top-by-delta
- Add optional classification overrides (quote/reply policy)
- Add lightweight regression fixtures for summary output

## Later

- Optional daily trend panel (window-over-window sparkline)
- Optional local scheduled run helper (cron-friendly wrapper)
